var path = require('path'),
	wd = null;

function getWD(){
	if(wd===null) {
		wd = process.cwd();
	}

	var args = [wd].concat(Array.prototype.slice.call(arguments));
	return path.join.apply(null, args);
}

module.exports = getWD;